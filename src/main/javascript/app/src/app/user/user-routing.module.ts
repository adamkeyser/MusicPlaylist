/**
 adduser/playlists: navigating the user to show all the playlists for a user //extra
 adduser/playlists/createplaylist: navigating the user to create a new playlist
 adduser/playlists/editplaylist: navigating the user to edit a playlist (add/delete a song)
 */




/**
import {NgModule} from "@angular/core";
import {Routes, RouterModule} from "@angular/router";
import {UserRegisterComponent} from "./user-register/user-register.component";

const routes: Routes = [
 /*** {
    path: 'adduser',
    component: UserRegisterComponent,
  }
];

@NgModule({
  imports: [RouterModule.forChild(routes)],
  exports: [RouterModule]
})
export class UserRoutingModule {
}
 */
